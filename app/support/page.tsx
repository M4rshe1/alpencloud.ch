import Support from "@/components/pages/support";
import type {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Alpencloud - Support',
    description: 'Melden Sie sich bei Fragen oder Problemen',
}

const Page = () => {
    return <Support/>
}

export default Page;